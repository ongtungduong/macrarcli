version: 2

# Explicit override: GoReleaser otherwise infers {{ .ProjectName }} from the
# git remote/repo directory name. The GitHub repository itself has not been
# renamed yet (a manual, external step — see README/CONTRIBUTING) so without
# this override, archive names would silently keep the old "rar2zip" name.
project_name: macrarcli

before:
  hooks:
    - go mod tidy

builds:
  - id: macrarcli
    main: .
    binary: macrarcli
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}

archives:
  - id: macrarcli
    ids: [macrarcli]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    # Windows users expect a .zip (and Scoop requires it).
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE*
      - completions/*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

# Keyless cosign signature over checksums.txt: authenticity on top of the
# checksum's integrity. Uses the GitHub Actions OIDC token (id-token: write) —
# no private keys to store or rotate. Verifiers check ${artifact}.sig against
# ${artifact}.pem (the Fulcio-issued certificate).
signs:
  - cmd: cosign
    signature: "${artifact}.sig"
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--output-signature=${signature}"
      - "--output-certificate=${certificate}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

# NOTE: goreleaser deprecates `brews` (formulae) in favor of `homebrew_casks`.
# We intentionally keep the formula: Homebrew casks are largely macOS-only and add
# Gatekeeper quarantine friction, so migrating would regress Linux `brew` users and
# unsigned-binary UX on macOS. `brews` still works in the pinned v2.16.0 (deprecated,
# not removed); revisit the cask migration when goreleaser schedules removal.
brews:
  - name: macrarcli
    # Skip publishing to tap when token is not configured.
    skip_upload: "{{ if eq .Env.HOMEBREW_TAP_TOKEN \"\" }}true{{ end }}"
    repository:
      owner: ongtungduong
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/ongtungduong/macrarcli"
    description: "Extract RAR archives directly — pure-Go, no unrar required"
    license: "MIT"
    install: |
      bin.install "macrarcli"
    test: |
      system "#{bin}/macrarcli", "--version"

scoops:
  - name: macrarcli
    # Skip publishing to the bucket when no token is configured (mirrors brews).
    skip_upload: "{{ if eq .Env.SCOOP_TAP_TOKEN \"\" }}true{{ end }}"
    repository:
      owner: ongtungduong
      name: scoop-bucket
      token: "{{ .Env.SCOOP_TAP_TOKEN }}"
    homepage: "https://github.com/ongtungduong/macrarcli"
    description: "Extract RAR archives directly — pure-Go, no unrar required"
    license: MIT
